class Controller {
  static createTransaction(transactionData) {
    return createTransaction(transactionData);
  }

  static checkStatus(reference) {
    return getTransactionStatus(reference);
  }

  static getFileLink() {
    return getFileLink();
  }
}
